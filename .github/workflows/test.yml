name: Tests

on:
  pull_request:
  push:
    branches: [feature/*]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: pip install ".[test,vision,anthropic,gemini]"
      
      - name: Smoke test - Import library
        run: python -c "from Agent import AgentKeywords; print('✅ Import successful')"